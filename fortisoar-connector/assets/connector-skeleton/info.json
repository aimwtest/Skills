{
  "name": "sample-connector",
  "version": "1.0.0",
  "label": "Sample Connector",
  "description": "Sample connector skeleton. Replace this description with what the target product/API does.",
  "publisher": "Community",
  "cs_approved": false,
  "cs_compatible": true,
  "icon_small_name": "small.png",
  "icon_large_name": "large.png",
  "category": "Miscellaneous",
  "help_online": "",
  "configuration": {
    "fields": [
      {
        "title": "Server URL",
        "name": "base_url",
        "type": "text",
        "required": true,
        "editable": true,
        "visible": true,
        "value": "https://api.example.com",
        "placeholder": "https://api.example.com",
        "tooltip": "Base URL of the target API. Must start with http(s):// and not end with a trailing slash.",
        "description": "Base URL of the target API server.",
        "validation": {
          "pattern": "^https?://[^/]+(?:/[^/]+)*$",
          "patternError": "Server URL must begin with http(s):// and must not end with a trailing '/'."
        }
      },
      {
        "title": "API Key",
        "name": "api_key",
        "type": "password",
        "required": true,
        "editable": true,
        "visible": true,
        "tooltip": "API key used to authenticate against the target API.",
        "description": "API key used to authenticate against the target API. Stored write-only."
      },
      {
        "title": "Verify SSL",
        "name": "verify_ssl",
        "type": "checkbox",
        "required": false,
        "editable": true,
        "visible": true,
        "value": true,
        "description": "Specifies whether the SSL certificate for the server is to be verified. Defaults to true."
      }
    ]
  },
  "operations": [
    {
      "operation": "get_status",
      "title": "Get Status",
      "description": "Retrieves service status from the target API. Replace with a real operation.",
      "category": "investigation",
      "annotation": "get_status",
      "enabled": true,
      "is_config_required": true,
      "parameters": [],
      "output_schema": {
        "status": "",
        "version": ""
      }
    }
  ]
}
